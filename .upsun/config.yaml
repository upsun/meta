applications:
  backend:
    type: nodejs:24

    build:
      flavor: none

    hooks:
      build: |
        set -ex
        cd backend
        npm install
        npm run build

    web:
      commands:
        start: cd backend ; npm start

      locations:
        /:
          root: public
          passthru: true
          allow: false
